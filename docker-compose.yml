services:
  db:
    image: mysql:8.3
    volumes:
      - ./mysql_data:/var/lib/mysql
      - ./initdb:/docker-entrypoint-initdb.d:ro
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: dev1
    ports:
      - 3306:3306
